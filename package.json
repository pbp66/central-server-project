{
	"name": "08-central-server-project",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"type": "module",
	"scripts": {
		"start": "node --experimental-modules --es-module-specifier-resolution=node server",
		"dev": "npx nodemon server",
		"seed": "node --experimental-modules --es-module-specifier-resolution=node seeds",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/pbp66/central-server-project.git"
	},
	"keywords": [],
	"author": "pbp66",
	"license": "AGPL",
	"bugs": {
		"url": "https://github.com/pbp66/central-server-project/issues"
	},
	"homepage": "https://github.com/pbp66/central-server-project#readme",
	"dependencies": {
		"dotenv": "^16.0.3",
		"express": "^4.18.2"
	},
	"devDependencies": {
		"nodemon": "^2.0.20"
	},
	"nodemonConfig": {
		"execMap": {
			"js": "node --experimental-modules --es-module-specifier-resolution=node"
		}
	}
}
